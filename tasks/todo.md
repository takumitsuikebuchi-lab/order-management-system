# タスク管理

作業開始時に計画を書き出し、完了したらチェックを入れる。
過去のタスクは削除せず、日付ごとにセクションを分けて残す。

---

## テンプレート

新しい作業を始めるときは以下をコピーして使う:

```
## YYYY-MM-DD: [作業タイトル]

### 背景・目的
（なぜこの作業が必要か）

### 計画
- [ ] ステップ1
- [ ] ステップ2
- [ ] ステップ3
- [ ] 動作確認

### 結果
（完了後に記録）
```

---

## 2026-04-01: ドキュメント整備・CLAUDE.md作成

### 背景・目的
プロジェクト全体のファイルを精査し、Claude Codeが正しく動作するための
CLAUDE.mdと、タスク管理用のtodo.mdを整備する。

### 計画
- [x] プロジェクト全体の構造を確認
- [x] CLAUDE.md（プロジェクト直下）を作成
- [x] tasks/todo.md を作成
- [x] メモリファイルを作成（将来の会話への引き継ぎ用）

### 結果
- CLAUDE.md: 作業前の読むべきファイル順・禁止事項・ルール・落とし穴を記載
- tasks/todo.md: テンプレートと今回の作業ログを記録
- メモリ: プロジェクト概要・ユーザープロファイルを保存

---

## 2026-04-01: データ保護ルール追記・月次自動バックアップ設定

### 背景・目的
受注履歴・業務記録の消失は会社にとって重大な損害になるため、
Claudeへの指示にデータ保護ルールを明記し、月末の自動CSVバックアップを設定する。

### 計画
- [x] CLAUDE.md にデータ保護の最重要ルールを追記
- [x] `.github/workflows/monthly-backup.yml` を作成（月末自動バックアップ）
- [x] `backups/` フォルダを作成（バックアップ保存先）

### 結果
- CLAUDE.md: データ削除禁止・スキーマ変更ルール・バックアップ確認義務を5項目で明記
- monthly-backup.yml: 毎月末日の深夜0時（JST）に受注明細・顧客マスタをCSV保存
- backups/: 例）`backups/2026-03_受注明細.csv` の形式で蓄積される
- 手動実行（緊急バックアップ）にも対応（GitHubのActionsタブから実行可能）
- 完全無料（公開リポジトリのためGitHub Actions使い放題）

---

## 2026-04-02: 顧客マスタ新規登録がリロード後に消える不具合の修正

### 背景・目的
「＋ 新規追加」で顧客を登録してもリロードすると消える不具合が報告された。
原因調査・修正をClaudeとCodexで分担して対応。

### 計画
- [x] ブラウザで動作確認・原因調査
- [x] cloudSaveCustomers の DELETE URL長すぎ問題を修正（`?id=not.is.null` 方式へ）
- [x] cloudFetchCustomers に `&limit=10000` を追加
- [x] saveNewCustomer を async/await 化して保存完了を待つよう修正
- [x] Supabase の重複30,847件をクリーンアップ（削除→正しい1,000件を再投入）
- [x] Codex が根本原因を特定・修正（`cloudInsertCustomer` 新設、ページング取得）
- [x] ドキュメント更新（AGENTS.md・CHANGELOG.md はCodex、lessons.md・todo.md はClaude）

### 結果
- 根本原因: 1件追加のたびに全件DELETE+再INSERTしていた設計 + Supabase 1,000件上限 + 非同期の競合
- Codex の修正: `cloudInsertCustomer()`（1件だけInsert）を新設し saveNewCustomer から呼ぶ方式へ変更
- Codex の修正: `cloudFetchCustomers()` を Range ヘッダによるページング取得に変更
- Supabase の customers テーブルを重複838件削除し163件に正常化
- 詳細な教訓は `tasks/lessons.md` の2026-04-02エントリに記録済み

---

## 2026-04-13: 未選択時の引取書を空伝票で印刷可能にする

### 背景・目的
受注を1件も選んでいない状態でも、空欄の引取書をそのままプレビュー・印刷したい要望が出た。
既存の選択済み印刷は維持したまま、未選択時だけ空伝票を安全に出せるようにする。

### 計画
- [x] 引取書印刷の未選択時分岐を空伝票出力へ変更
- [x] 空伝票で日付や宛先表示が崩れないよう整形を追加
- [x] Playwright smoke test に未選択時の印刷確認を追加
- [x] 動作確認

### 結果
- 未選択で `引取書` を押したとき、空欄の「貨物引取書・荷渡書」を1枚プレビューできるように変更
- 日付未設定時に `NaN/NaN/NaN` が出ないようにし、空欄時は `様` だけが残らないよう整形を追加
- 選択済み印刷の挙動は維持しつつ、未選択時の回帰防止テストを追加
- ボタン表示も実際の挙動に合わせて `📋 引取書` へ修正

---

## 2026-04-13: 週次自動バックアップCSVの文字化け対策

### 背景・目的
GitHub Actions の週次バックアップで保存された CSV を Excel で直接開くと、日本語が文字化けして見える状態だった。
データ自体は UTF-8 だが BOM がなく、Windows 環境で判定を誤りやすいため、バックアップの可読性を上げる。

### 計画
- [x] 週次バックアップの CSV 生成方法を確認
- [x] 自動バックアップ出力に UTF-8 BOM を付与
- [x] ローカルで出力形式を確認
- [ ] 反映確認

### 結果
- 週次バックアップは Supabase の `text/csv` をそのまま保存しており、UTF-8 だが BOM なしだった
- ワークフロー内で一時ファイルに保存後、UTF-8 BOM を先頭に付けて `backups/` へ出力するよう修正
- ローカル検証で先頭バイトが `EF BB BF` になることを確認

---

## 2026-04-13: UIレビュー指摘のアクセシビリティ改善

### 背景・目的
レビューで、モーダルの Escape 動作、フォームラベルの関連付け、
オートコンプリートのキーボード操作、キーボードフォーカス表示に改善点が見つかった。
保存・同期ロジックには触れず、データ破損や消失のない範囲で UI だけを安全に改善する。

### 計画
- [x] Escape キーで全モーダルを一貫して閉じられるよう修正
- [x] フォームラベルと入力欄の関連付けを追加
- [x] オートコンプリートにキーボード操作と ARIA 属性を追加
- [x] ボタン・セレクトのキーボードフォーカス表示を追加
- [x] Playwright smoke test を追加
- [x] 動作確認

### 結果
- `Escape` でクラウド設定・運行指示書設定を含む全モーダルを閉じられるよう統一
- 受注フォーム、クラウド設定、顧客追加、運行指示書設定で `label for` を追加
- 顧客名・引取先・配送先の補完候補に `combobox/listbox` の ARIA 属性、上下キー移動、Enter 決定、Escape/Tab での終了を追加
- ボタン、セレクト、閉じるボタン、チェックボックスに可視フォーカスを追加
- `tests/smoke.spec.js` に Escape 動作とラベル/キーボード補完の確認を追加
- `npm run test:ui` で 36 件すべて成功

---

## 2026-04-17（予定・18時以降着手）: 長期運用改善ロードマップ

### 背景・目的
システム全体のレビュー結果を踏まえ、長期運用に耐える構造へ段階的に改善する。
業務時間中は入力担当者と競合してデータが壊れるリスクがあるため、夕方18時以降に実行する。

### 合意済みの前提
- マスタ上限は200件程度（スケーラビリティ懸念は主に受注側）
- TypeScript化はしない（運用破綻リスク回避）
- ファイル分割は遠い将来 CSS → utils.js → JSDoc の順で段階的に
- inline onclick/onchange は絶対に変更しない（過去の分割失敗ゾーン）
- 各項目完了後に `npm run test:ui` で37件通過確認

### 計画（優先順）
- [x] 1. `Date.now()` ID撤廃 → Supabase `gen_random_uuid()` に一本化
- [x] 2. orders テーブルに `updated_at` 追加＋競合検知強化（スキーマ変更は事前確認）
- [x] 3. 削除系・1,500件ページング・部分障害テストの追加
- [x] 4. IME・印刷内容・月跨ぎのテスト追加
- [x] 5. 受注一覧デフォルトを「当月＋前月」に、全件ボタン追加
- [x] 6. localStorage 容量監視（3MB超で警告）
- [x] 7. 週次バックアップの3年超自動削除
- [x] 8. CI に cloud-config と index.html フォールバック同期チェック
- [x] 9. CSS外部ファイル化（styles.css 切り出し）
- [x] 10. 純関数ユーティリティを utils.js に抽出
- [x] 11. JSDoc + `// @ts-check` の段階導入

### 結果
- 1. `generateLocalId()` ヘルパー（`crypto.randomUUID()` + フォールバック）を追加し、ID生成4箇所（loadData補完・applyFetchedOrders補完・saveOrder新規・CSV取込）を UUID 化。同時に UUID 文字列では壊れる `a.id - b.id` ソートを `orderNo` の日本語 localeCompare に修正。Playwright 37件 通過。
- 2. Supabase `orders` に `updated_at timestamptz not null default now()` を追加＋`set_updated_at()` トリガー設定（SQL Editor で実行確認済み）。`normalizeOrderRow` に `updatedAt` を取り込み、`editSessionBaseline` に保存。`saveOrder` の競合検知を「updated_at が前進 OR 署名相違」で判定する二重チェックに強化。`schema.sql` も同期。Playwright 37件 通過。
- 3. スモークテストを 37件 → 40件 に拡張。追加: (a) クラウドモードで削除ボタンが DELETE リクエストを発行する、(b) 1,500件のデータが Range ヘッダーで 2 ページに分けて全件取得される、(c) DELETE 失敗時にキューへ積まれ `flushCloudQueue()` で復旧する。
- 4. さらに3件追加して 43件 に。(a) IME 変換中は顧客検索が絞り込まれず、compositionend で初めて適用される、(b) 運行指示書の印刷 HTML に積地/卸地住所・品名・数量・車両が含まれる、(c) 12月→翌年1月の月送り/戻りで受注が混ざらない。
- 5. `viewMode` state（`range`/`all`/`single`）を導入。既定は `range`（当月＋前月）で、一覧の過去参照が1操作で広がる。バッジは「2026年3月〜4月」のように表示し、📚全件表示ボタンで全期間一覧、月ナビで自動的にレンジへ戻る。統計カード（件数・金額・未完了）は従来どおり当月単体を維持。CSV出力・請求書CSVは `single` モードを明示的に指定し、従来どおり選択月のみ出力。
- 6. `LOCAL_STORAGE_WARN_BYTES = 3MB` 閾値で `saveData` 実行時に UTF-16 バイト長を合算し、初回超過時のみトースト警告。多重通知で業務を妨げないよう `localStorageWarnShown` フラグで一度きり。
- 7. `.github/workflows/weekly-backup.yml` に「古いバックアップを削除（3年超）」ステップを追加。mtime は git 操作で変わるためファイル名先頭の `YYYY-MM-DD` を正規表現で抽出、3年より古いものだけ `git rm` する安全側設計。
- 8. `.github/workflows/guard-and-sync.yml` の index.html 検証を強化。`EMBEDDED_SHARED_CLOUD_CONFIG` ブロックだけを抽出して `url` / `anonKey` / `enabled` の3項目すべてが `cloud-config.json` と一致することを CI で担保（フォールバック値のズレを事故前に検知）。
- 9. `<style>` ブロック約1,190行を `styles.css` に切り出し、`<link rel="stylesheet" href="styles.css">` で読込。ブラウザキャッシュ効率と可読性が向上。
- 10. 純関数8つ（`generateLocalId` `escHtml` `toNumber` `fmtDate` `formatDate` `parseCsvLine` `csvQ` `normalizeForSearch`）を `utils.js` に抽出。`index.html` 側の重複定義は削除。classic script（`type="module"` は使わない）で inline onclick と衝突しない安全な切り出し。
- 11. `utils.js` 冒頭に `// @ts-check` を宣言し、抽出した全関数に JSDoc 型注釈（`@param` / `@returns`）を付与。非エンジニアでも VS Code 上で型エラーと補完が効く段階導入。
- 月ナビボタン（←/→）に `aria-label="前月へ"`/`"次月へ"` を付与し、スモークテストを `getByRole({ name: '前月へ' })` 等の a11y 名でのセレクタに更新（43件すべて通過）。

---

## 2026-04-17: QA指摘のまとめ対応＋週次バックアップを全マスタへ拡張

### 背景・目的
前作業（絞り込みバー再構成）のQAレビューで挙がった CRITICAL / HIGH / MEDIUM 指摘を一通り解消し、
週1回の自動バックアップを「受注明細・顧客マスタ」に加え、品名・荷姿・単位・ドライバー・車両などの
シンプルマスタも含めたすべての業務データに拡張する。

### 計画
- [x] 週次バックアップ（`.github/workflows/weekly-backup.yml`）に品名・荷姿・単位・ドライバー・車両・シンプルマスタ全体のCSV出力を追加
- [x] C-1 `.filter-clear` の文字色を濃色にしてアンバー背景とのコントラストをWCAG AAへ改善
- [x] H-1 ドライバー・車両フィルタを完全一致判定に変更（顧客検索は部分一致のまま）
- [x] H-2 モバイル表示で `.filter-clear` のタップ領域を 44px 以上に確保
- [x] H-3 `.filter-clear` のフォーカス表示を2段構え（内側白／外側青）に変更
- [x] H-4 Playwright モックの simple_masters に車両データを追加
- [x] M-1 `updateStatsFromView` で「本日配送・翌日配送」件数も表示中行から再集計
- [x] M-2 `saveOrder` のフィルタ保存ロジックを `captureTableFilterState/restoreTableFilterState` 共通処理に統一（車両保持バグ解消）
- [x] M-3 顧客検索の IME 入力中はフィルタを発火させない（日本語入力の取りこぼし防止）
- [x] M-4 ドライバー・車両セレクトの最小幅を 180px に統一
- [x] M-5 絞り込み中の「●」マークを文字サイズ小さめ＋余白調整で上品に
- [x] M-6 クリアボタンに `aria-label` を付与（視覚ラベルを含めて WCAG label-in-name 準拠）
- [x] M-7 並び順セレクトの左に `border-left` の仕切りを追加
- [x] M-8 `.filter-input:focus-visible` をフォームコントロール共通に統一
- [x] `npm run test:ui` で 37 件全成功を確認

### 結果
- 週次バックアップは `受注明細 / 顧客マスタ / 品名 / 荷姿 / 単位 / ドライバー / 車両 / シンプルマスタ全体` の8種類を UTF-8 BOM付き CSV で毎週保存するように
- 絞り込みバーの見た目・操作性・アクセシビリティを総合的に改善（コントラスト・タップ領域・フォーカス表示・IME対応・キーボード操作）
- H-1 修正で「山田」を選んだときに「山田太郎」の行が混ざらないように（顧客検索の部分一致は維持）
- saveOrder の手書き復元ロジックを共通化して車両フィルタ保持バグも同時解消

---

## 2026-04-17: 絞り込みバーの整理と車両ソートの追加

### 背景・目的
トップの絞り込み行でドライバー・日付のクリアが効かず（「クリア」ボタンが顧客検索専用だったため）、
また車両での絞り込み機能がなかった。UI自体も見づらいので、4項目の絞り込みを並べ直し、
1つの「すべてクリア」で全解除できる形に整理する。

### 計画
- [x] HTML：顧客検索／日付／ドライバー／車両／並び順 を `filter-bar` に再構成
- [x] CSS：絞り込み中の項目に青枠＋●マークでハイライト、レスポンシブ対応
- [x] JS：`setupFilters` に車両セレクト生成を追加、`filterTable` に車両条件を追加
- [x] JS：`clearAllFilters` 新設、旧 `clearDateFilter`/`clearCustomerFilter` は互換維持
- [x] JS：`captureTableFilterState`/`restoreTableFilterState` に車両を追加（同期後の保持）
- [x] Playwright：全クリアで4項目戻る／車両フィルタで絞れる の2テスト追加（旧日付解除テストは置換）
- [x] 動作確認（`npm run test:ui` 37件成功）

### 結果
- 絞り込み行が「顧客検索／日付／ドライバー／車両／並び順」＋「🗑 すべてクリア」1本に統一
- ドライバー・日付もクリアで必ず解除される（従来のバグ解消）
- 車両プルダウンで絞り込めるようになった
- 絞り込み中は枠が青色＋ラベルに●が付いて、何で絞っているか一目で分かる

---

## 2026-04-19: 長期ロードマップ完了・運用観察フェーズ入り

### 背景・目的
2026-04-17 起案の長期ロードマップ（全11項目）が完了し、ID一本化・更新時刻による競合検知・スモークテスト拡張（37→43件）・
レンジ表示・容量監視・バックアップ自動削除・CI同期チェック・CSS/utils分離・JSDoc段階導入まで一通り着地した。
ここで一度立ち止まり、新機能追加を控えて現場での実運用を観察する期間に入る。

### 観察ポイント（1〜2週間）
- [ ] 📚 全件表示ボタンが実際に使われているか、/一覧の表示範囲が業務感覚とずれていないか
- [ ] localStorage 3MB 警告トーストが出た端末があれば報告を受け、原因（画像埋め込み・ログ肥大等）を特定
- [ ] 週次バックアップが正常に走り続けているか（自動削除ステップのログで削除件数を毎週確認）
- [ ] CI の EMBEDDED_SHARED_CLOUD_CONFIG 検証が意図せぬブロックを起こさないか
- [ ] styles.css / utils.js 分離後にブラウザキャッシュ由来の表示不整合が起きないか

### 観察中にやらないこと
- 新しい大型機能・設計変更（小バグ修正と業務追随は別）
- 壊れていない既存フローのリファクタリング
- パフォーマンス最適化（実測で問題が見えてから）

---

## 2026-07-17: 行選択チェックの保持修正＋クイックバー追加

### 背景・目的
一覧の行選択チェックが「30秒〜1分で勝手に外れる」不具合の報告。原因は60秒ごとの
クラウド同期による一覧再描画で、フィルタは復元されるがチェック選択は復元されていなかった。
あわせて、下までスクロールすると新規作成等のボタンに戻るのが手間という要望に対し、
スクロール時のみ表示されるクイックバー（＋先頭へボタン）を追加する。

### 計画
- [x] renderTable にチェック選択の退避・復元を追加（60秒同期で消えないように）
- [x] クイックバー（＋新規受注／運行指示書（選択分）／引取書／⬆先頭へ）を追加
- [x] 業務ボタン1行目が画面外に出たときだけ表示する制御（scroll+resize監視）
- [x] スモークテスト2件追加（選択保持・クイックバー表示/先頭へ）＋既存セレクタのスコープ化
- [x] npm run test:ui 45件全成功
- [x] 実ブラウザ確認（デスクトップ1280・モバイル375の両幅で目視）

### 結果
- 選択チェックは再描画後に受注IDベースで復元される（表示中に残っている受注のみ）
- クイックバーは position:fixed / z-index:900（モーダル1000より下）。初期状態は非表示
- IntersectionObserver 方式は高速スクロールで発火しないケースがあるため scroll イベント方式を採用
- モバイルではアクションバー全体が縦長になるため、判定基準は「業務ボタンの1行目」に設定

---

## 2026-07-17: クイックバーを2段構成に拡張（マスタ系＋検索系）

### 背景・目的
クイックバー導入後、ユーザーから「マスタボタンと検索機能もバーに入れて2段構成に、
先頭へボタンは大きく」という要望。

### 計画
- [x] 1段目: 業務3ボタン｜仕切り｜マスタ6ボタン｜大きめの「⬆先頭へ」（右端）
- [x] 2段目: 絞り込みバー（本物のDOMを移動・複製しない＝入力値とIDの二重化を回避）
- [x] 元の位置に同高スペーサーを置いてレイアウトジャンプを防止
- [x] モバイル（<768px）は従来のコンパクト表示（マスタ・検索は非表示）
- [x] バー内にフォーカスがある間は隠さないガード（絞り込みで一覧が縮んだ瞬間に検索欄が消える問題の対策）
- [x] テスト更新（マスタボタン重複に伴うセレクタスコープ化・移動/復帰の検証追加）45件成功
- [x] 実Chromiumで1280/375の両幅を目視確認

### 結果
- バー内の検索欄はそのまま絞り込みに使え、先頭へ戻ると絞り込みバーは元の位置に戻る
- 先頭へボタンは onclick で blur してから scroll（フォーカスガードと両立させるため）

---

## 2026-07-17: 先頭へボタンの円形化＋一覧の横幅ズレ修正

### 背景・目的
ユーザーレビューで (1) 先頭へボタンが不格好で車両マスタの隣に並んでしまう、
(2) 受注一覧の白枠が上のボックス類と左右が揃っていない、の2点の指摘。

### 計画
- [x] 先頭へボタンを円形48px・新規受注と同じ青・右上固定（position:fixed）に変更
- [x] クイックバーに padding-right を設けてボタン列との重なりを防止
- [x] table-wrapper を width:calc(100% - 2rem)＋中央寄せで上のボックスと左右を統一
- [x] stats-grid にも max-width:1800px＋中央寄せ（超ワイド画面での統一）
- [x] テスト45件成功＋1280/1900/375の3幅で左右座標の一致を数値確認・目視確認

### 結果
- 1280px・1900px とも action-bar / table-wrapper / stats-grid の左右座標が完全一致
- 円形ボタンは aria-label="先頭へ戻る" で既存テストのセレクタ（/先頭へ/）も互換

---

## 2026-07-17: 一覧見出し行のデザイン改善

### 背景・目的
列タイトル行の背景が var(--bg-main)＝ページ背景と同色（#e5e7eb）で溶けて見えづらい、
という指摘。見出しであることが一目で分かるように。

### 計画・結果
- [x] 見出し行をダークネイビー #334155＋白文字に変更（下線 #1e293b）
- [x] th 間に薄い白の仕切り線（rgba 255,255,255,0.18）で列の区切りを明示
- [x] 全選択チェックボックスに accent-color を指定してダーク背景でも視認可能に
- [x] テスト45件成功・1440pxで目視確認

---

## 2026-07-17: 見出し文字の中央揃え＋CSSキャッシュ自動更新

### 背景・目的
(1) 見出し行の文字を上下左右中央揃えに。(2) 別ブラウザでCSS変更が反映されない
（キャッシュ残留）問題への恒久対策。

### 計画・結果
- [x] .data-table th を text-align:center / vertical-align:middle に
- [x] styles.css / utils.js の読み込みに ?v=20260717a を付与（変更時に数字を上げれば
      全端末のキャッシュが自動更新される。ハードリロード指示が不要に）
- [x] テスト45件成功・目視確認

---

## 2026-07-17: 見出し3列の縦ズレ修正＋帯の端の切れ解消

### 背景・目的
(1) 顧客名・引取先・配送先の見出しだけ上寄せになっていた（列折り返し用の旧ルール
vertical-align:top が th にも効いていた）。(2) 白枠の内側余白のせいで見出しの帯が
枠の途中で切れて見えていた。

### 計画・結果
- [x] thead の 3〜5 列目だけ vertical-align:middle を上書き（td の折り返し上寄せは維持）
- [x] .table-wrapper の左右 padding を 0 にして帯を枠の端から端まで（角丸に沿って収まる）
- [x] キャッシュバスターを 20260717b に更新
- [x] テスト45件成功・左右端のクローズアップで目視確認

---

## 2026-09-04: 消費税の計算をマネーフォワードに合わせる（請求書単位で四捨五入）

### 背景・目的
売上高（税込）がMFの請求書合計と数円ずれる月があった（2026年7月・8月で各6円）。
原因は、本システムが受注1件ごとに税込額を四捨五入して足していたのに対し、MFは
顧客ごとの税抜合計に対して消費税を1回だけ四捨五入するため。

### 計画・結果
- [x] `utils.js` に `calcInvoiceTax` / `calcInvoiceTotals`（顧客×月で税を1回四捨五入）を追加
- [x] 売上高カード（初期表示・絞り込み後）を請求書単位の集計に変更、消費税額も表示
- [x] 請求書CSVの「消費税」「合計金額」を同じ方式に変更（品目行・受注1件の税込欄は従来どおり）
- [x] utils.js のキャッシュバスターを 20260904a に更新
- [x] テスト2件追加（.5円が出るデータで 3,824→3,823 になること／CSVの消費税列）、47件成功
- [x] 本番データで検算: 2026-07 は 3,851,473→3,851,467、2026-08 は 5,300,596→5,300,590（MFの四捨五入計算と一致）
- 備考: 単価×数量≠金額(税別)の受注が2件あり（R260130-004: 200×11=2,200 vs 2,198 / R260903-014: 1,500×32=48,000 vs 47,998）。MFは単価×数量で再計算するため、これは税でなく入力の差

---

## 2026-09-04: 金額欄がホイール操作で2円減る事故の修正

### 背景・目的
R260903-014（梶原農場）の金額(税別)が 1,500×32=48,000 のはずが 47,998 で保存されていた
（登録時刻＝更新時刻なので登録時点でこの値）。Playwrightで再現: 金額欄にカーソルを置いたまま
ホイールを2回回すと 47,998 になる（Chrome標準の number input の挙動）。2026-02 の R260130-004
（2,200→2,198）も同じ型。

### 計画・結果
- [x] 数量・単価・金額(税別)の入力欄に `onwheel="this.blur()"` と ↑↓キー無効化を追加
- [x] 再現テストを追加（修正前に失敗＝47998、修正後に成功）、48件成功
- [ ] R260903-014 の金額を 48,000 に直す（ユーザーが編集画面から実施）
- 備考: 受注番号は「登録日」で採番する現仕様。配送日に合わせて採番し直す案はユーザー判断で見送り（2026-09-04、現状維持で確定）

---

## 2026-09-04: リポジトリ全体の整合性点検（ドキュメント・設定・CI・スキーマ）

### 背景・目的
ユーザー依頼「最新の状態に不整合がないか、README含め全ファイルを確認」。サブエージェント2体で
ドキュメント↔コード、設定・CI・スキーマ↔コードを照合し、実物で一次検証してから修正。

### 計画・結果
- [x] ドキュメント17項目を修正（クリア/日付解除→すべてクリア、PDFプレビュー→運行指示書、AGENTS.md参照→CLAUDE.md、
      車両フィルタ・全件表示・月次レポートPDF・CSV保存先の追記、SETUP.md の古いSQL全文を schema.sql 参照に、
      README のポート 8000→4173、バックアップ対象に簡易マスタ6種、CHANGELOG を 2026-04-13〜09-04 まで追補、対象時点の日付更新）
- [x] index.html:1796 受注取得の order パラメータ二重指定を `order=date.asc,order_no.asc` に修正（本番で二重指定だと2つ目が効かないことを実測）、テストの route 文字列4箇所も同時修正
- [x] .gitignore に .DS_Store / .claude/ を追加
- [x] テスト48件成功
- [x] 【対応済 2026-09-04】リポジトリが Public で、backups/ と CSV保存/ の顧客マスタ（住所・電話番号）・受注明細が
      GitHub と本番URL（GitHub Pages は全ファイルを配信）から誰でも取得できる。非公開化（個人アカウントは GitHub Pro 必要）＋
      配信物からバックアップを外す（Pages を Actions 配信にしてアプリファイルだけ載せる／バックアップ先を別の非公開リポへ）＋履歴の削除
- [x] 【対応済 2026-09-04・追跡をやめ kyoshin-order-backups/csv-exports へ移設】CSV保存/ の未追跡7ファイル＋変更1ファイル（22列の新形式で再出力済み）をコミットするか、追跡をやめるか（上の公開範囲の判断が前提）
- [ ] 【提案】顧客マスタCSV（出力・取込）が3列で `delivery_address`（配送先住所）を落とす → 4列化（取込は3列も後方互換）
- [ ] 【提案】schema.sql にインデックスが無い → `orders(date)`、`simple_masters(master_type, sort_order)`、`customers(customer_name)`
- [ ] 【提案】setup-wizard.html は本番では到達不能（cloud-config.json が enabled のため）→ 復旧経路として残すなら README に明記、不要なら削除
- [ ] 【提案】manual_images/ の5画像はどこからも参照されておらず、`01-welcome.webp-01-welcome.webp` は名前が壊れている → 使うか消すか
- [ ] 【提案】guard-and-sync.yml の pull_request トリガが main 宛のみ（master 宛PRは未検証で通る）
- 備考: ローカル master ブランチは97コミット遅れだが origin/master は origin/main と同一。週次バックアップの3年超削除は未発動（最古 2026-04-08）

---

## 2026-09-04: 顧客データを公開リポジトリから除去（方針2＝公開のまま、データだけ外へ）

### 計画・結果
- [x] 書き換え前の完全ミラーを `~/arenge-work/clients/01-kyoshin/_backup-order-management-system-mirror-20260904.git` に保管
- [x] 非公開リポジトリ `kyoshin-order-backups` を作成し、backups/ 166件＋CSV保存/ 10件を移設（SHA-256 全件一致を確認）
- [x] weekly-backup.yml をデプロイキー経由で非公開リポへ push する方式に変更（この公開リポへの書込権限は外した）
- [x] backups/ と CSV保存/ を git 追跡から外し .gitignore に追加（CSV保存/ はアプリの保存先としてローカルに残す）
- [x] README / SETUP / CLAUDE.md / CHANGELOG を更新
- [x] git filter-repo で main/master の履歴から backups/ と CSV保存/ を除去し force-with-lease で push（171→148コミット。旧SHA 3a3f530 は GitHub のキャッシュで当面参照可＝サポート依頼で消す）
- [x] 検証: 本番URLで backups/・CSV保存/ のCSVが404（Pages は force push では自動再ビルドされず、`gh api -X POST .../pages/builds` で明示要求して反映）、GitHub API で backups を触ったコミット0件、Weekly Backup 手動実行 success → kyoshin-order-backups に 2026-09-04 の8ファイル追加（計174件）
- [ ] GitHub サポートへ「削除済みデータのキャッシュ消去」依頼（ユーザー操作。依頼文は下書き済み）
- 備考: 残る課題＝Supabase anon key が公開ページに埋め込まれ RLS が全許可（DB自体は誰でも読み書き可）。別途対策が必要
