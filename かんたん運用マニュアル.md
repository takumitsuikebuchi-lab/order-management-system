# 受注管理システム かんたん運用マニュアル

## はじめに

このシステムは、受注情報と各種マスターを複数のパソコン・ブラウザで共有できる受注管理システムです。
2026年3月時点の本番運用では、公開URLを開くだけで共通のクラウド設定が自動適用されるようになっています。

### 本番URL

`https://takumitsuikebuchi-lab.github.io/order-management-system/`

### このシステムでできること

- 受注の登録・編集・複製・削除
- 顧客、積荷、荷姿、単位、ドライバー、車両のマスタ管理
- PDFプレビュー、運行指示書、引取書、CSV出力
- 複数PC・複数ブラウザでのクラウド共有

---

## 1. 最初に確認すること

### 通常運用

1. 本番URLを開く
2. 数秒待つ
3. 右上が `接続: Cloud（同期完了）` になることを確認する
4. 受注一覧とマスターが表示されれば、そのまま使い始めてよい

### 大事なポイント

- 現在は `cloud-config.json` の共通設定が自動で読み込まれます
- 通常は、画面の「クラウド設定（Supabase）」を編集する必要はありません
- 新しいPCや新しいブラウザでも、同じURLを開けば同じデータが見える前提です

---

## 2. 日常の使い方

### 受注を登録する

1. `＋ 新規受注` を押す
2. 必須項目を入力する
3. `保存` を押す

### 受注を編集・複製・削除する

- 各行の `編集` で修正
- 各行の `複製` で内容を引き継いで新規登録
- 各行の `削除` で受注を削除

### マスターを更新する

- 顧客マスタや各種マスタの画面で追加・修正・削除する
- `保存` を押すと、他の端末にも反映されます

### 同期状態を見る

右上表示の見方は次のとおりです。

- `接続: Cloud（同期完了）`: 正常
- `接続: Cloud（同期中）`: 読み込みまたは同期中
- `接続: Cloud（エラー）`: 通信または設定に問題あり
- `接続: ローカル`: クラウド設定が使われていない

---

## 3. よくあるケース

### 新しいPCや新しいブラウザで使いたい

本番URLを開いて、右上が `接続: Cloud（同期完了）` になれば準備完了です。
通常は手入力の設定は不要です。

### 追加した受注が他の端末に見えない

1. 右上表示を確認する
2. `接続: Cloud（同期完了）` でない場合は、通信または設定を疑う
3. ハードリロードする
4. それでも直らなければ `SETUP.md` のトラブルシューティングを参照する

### 同じ受注を別の端末でも開いていた

現在は軽量な競合検知が入っています。
別端末で先に更新されていると、保存時に警告が出て止まります。
その場合はいったん閉じて、最新の受注を開き直してください。

### マスターを全部削除した

現在は空の状態も正しいデータとして同期されます。
つまり、ある端末で全部削除して保存すると、他の端末でも空になります。

---

## 4. クラウド設定を変更したいとき

通常は画面から変更しません。
変更が必要な場合は、`cloud-config.json` を更新するのが正しい方法です。

### 正しい変更場所

- `cloud-config.json`

### 反映手順

1. `cloud-config.json` を編集
2. `main` に commit / push
3. GitHub Pages の反映を待つ
4. ブラウザをハードリロードする

### 緊急時だけ画面から変える方法

診断や一時復旧のために、次のURLでロック解除できます。

`https://takumitsuikebuchi-lab.github.io/order-management-system/index.html?manualCloudConfig=1`

これは一時対応専用です。
最終的な正しい設定は、必ず `cloud-config.json` に戻してください。

---

## 5. 困ったとき

### 右上が `接続: Cloud（エラー）` になる

- 回線状況を確認する
- ページをハードリロードする
- `cloud-config.json` が正しい Supabase を向いているか確認する

### データ差異が出た

- `SETUP.md` と `AGENTS.md` を確認する
- AIツールに相談するときは、「先に `AGENTS.md` と `SETUP.md` を読んでから対応して」と伝える

### バックアップ

- 定期的に CSV 出力を行う
- クラウド運用中でも月次の CSV バックアップを推奨

---

## 6. AIツールに相談するときの伝え方

Codex / Claude / Cursor などに依頼する場合は、次の情報を一緒に渡してください。

1. 本番URLは `https://takumitsuikebuchi-lab.github.io/order-management-system/`
2. 共通クラウド設定の正本は `cloud-config.json`
3. 通常運用ではクラウド設定UIはロックされている
4. まず `AGENTS.md` と `SETUP.md` を読むこと

これで、運用前提の誤解による再設定ミスが起きにくくなります。
