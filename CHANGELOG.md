# Changelog

このファイルは、運用上重要な変更を時系列で簡潔に記録するためのものです。

## 2026-09-04

- 消費税の計算をマネーフォワードに合わせ、受注1件ごとではなく顧客×月（請求書単位）の税抜合計に対して1回だけ四捨五入する方式へ変更（`utils.js` に `calcInvoiceTax` / `calcInvoiceTotals` を追加）
- 売上高（税込）カードと請求書CSVの「消費税」「合計金額」を同方式に統一（受注1件の税込欄は従来どおり）
- 受注フォームの数量・単価・金額(税別)欄で、マウスホイール／↑↓キーによる値の増減を無効化（48,000円が47,998円で保存されていた事故の再発防止）
- UIスモークテストを48件に拡張。`utils.js` のキャッシュバスターを `20260904a` へ更新

## 2026-07-17

- 一覧の行選択チェックが60秒ごとのクラウド同期で外れる不具合を修正（受注IDベースで退避・復元）
- 業務ボタンが画面外に出たときだけ上部に固定表示されるクイックバーを追加（業務3ボタン＋マスタ6ボタン＋絞り込み一式＋円形「先頭へ」）。モバイルはコンパクト表示
- 一覧見出し行をダークネイビー化・中央揃えにし、列の縦ズレと帯の端切れを修正
- `styles.css` / `utils.js` の読み込みに `?v=` キャッシュバスターを付与（変更時に番号を上げれば全端末に反映）
- UIスモークテストを45件に拡張

## 2026-05-26

- 「📑 月次レポートPDF」ボタンを追加（巡回指導用）。表示中の月の運行計画・実績一覧を A4 横の印刷専用レイアウトで新規ウィンドウに描き、印刷ダイアログからPDF保存する方式
- ファイル名は `きょうしん輸送_運行計画実績_令和XX年XX月` を既定値に設定（拡張子はブラウザが付与）。印刷後はウィンドウを自動クローズ

## 2026-04-18

- `styles.css`（UIスタイル）と `utils.js`（純関数ユーティリティ）を `index.html` から分離
- 「📚 全件表示」ボタンを追加（押すと全期間の受注を表示し「📅 月表示に戻る」にトグル。既定は当月表示のまま）
- `orders.updated_at` による端末間の編集競合検知を強化
- 週次バックアップに3年超のCSV自動削除を追加。localStorage 容量監視を追加
- 削除・1,500件ページング・部分障害リカバリ・IME・年跨ぎ月送りのテストを追加

## 2026-04-17

- 絞り込みバーを「顧客検索／日付／ドライバー／車両／並び順」に再構成し、解除を「🗑 すべてクリア」1本に統一（旧 `クリア` / `日付解除` は廃止。ドライバー・日付がクリアで解除されないバグも解消）
- 車両プルダウンでの絞り込みを追加。ドライバー・車両フィルタは完全一致判定
- 週次バックアップを受注明細・顧客マスタに加え、品名／荷姿／単位／ドライバー／車両／シンプルマスタ全体の6ファイルへ拡張（計8種類）
- QAレビュー指摘（コントラスト・タップ領域・フォーカス表示・IME入力中のフィルタ抑止・車両フィルタ保持）とアクセシビリティ改善（Escape閉じ・ラベル・キーボード補完）を一括対応
- ID生成を `crypto.randomUUID()` へ統一

## 2026-04-13

- 週次バックアップCSVに UTF-8 BOM を付与（Excel で直接開いても日本語が文字化けしないように）
- 受注を選択していないときも引取書を空伝票で印刷できるようにし、ボタン名を整理

## 2026-04-02

- 顧客マスタの「＋ 新規追加」で、追加直後は見えるがリロード後に消える不具合を修正
- 原因だった `customers` 全件の `DELETE → POST` 方式を新規追加フローから切り離し、1件追加成功後にだけ localStorage と画面へ反映するよう変更
- 顧客取得時の整形処理をまとめ、クラウド再読込時に顧客データの形をそろえるよう整理
- Supabase REST の 1000 件上限により顧客一覧が途中までしか読めていなかったため、`cloudFetchCustomers()` をページング取得へ修正
- 本番 `customers` テーブルの同名重複 838 件を事前バックアップ後に整理し、163 件へ正常化

## 2026-04-01

- `CLAUDE.md` を新設（Claude Codeがこのプロジェクトを正しく扱うための指示書。作業前の読込ファイル順・データ保護ルール・禁止事項を明記）
- `.github/workflows/weekly-backup.yml` を追加（毎週水曜深夜0時JST に受注明細・顧客マスタをCSV出力し `backups/` フォルダへ自動保存）
- `backups/` フォルダを追加（週次バックアップの蓄積先）
- `tasks/todo.md` を追加（タスク管理ログ）
- `README.md` のファイル構成・バックアップ説明を現状に合わせて更新

## 2026-03-19

- `requirements.md` を新設
- `TEST_CHECKLIST.md` を追加
- `RUNBOOK.md` を追加
- `schema.sql` を追加し、DB初期構築の正本を分離
- Playwright による UI スモークテストを追加
- GitHub Actions の verify に UI スモークテストを追加
- 既存ドキュメントから新しい正本資料への導線を追加
- UI smoke test を拡張し、受注CRUD、検索・絞り込み保持、月切替、統計カード、印刷対象、CSV入出力、クラウドキュー回復、空マスタ同期まで自動確認できるよう整理
- GitHub Actions 上で揺れた月切替系の CSV テストを、内部変数の直接書き換えではなく UI の月送り操作ベースに安定化

## 2026-03-18

- クラウド再同期時に一覧の絞り込み条件を保持するよう修正
- 受注一覧の検索UIを `顧客検索` 1欄に統一
- 顧客検索クリアを右端の `クリア` 1ボタンに整理
- 日付解除ボタンを `日付解除` に整理
- 関連ドキュメントを更新

## 2026-03-17

- GitHub のデフォルトブランチを `main` に変更
- `main` から `master` への自動同期ワークフローを追加
- 共有クラウド設定の復元と起動フォールバックを強化
- AIツール向けの保守メモを整理

## 2026-03-13

- UUID / 文字列 ID 環境での編集・複製・削除・PDFプレビュー不具合を修正
- ブラウザ差で同期や表示が崩れる問題の調査を実施
