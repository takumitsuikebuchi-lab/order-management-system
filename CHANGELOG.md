# Changelog

このファイルは、運用上重要な変更を時系列で簡潔に記録するためのものです。

## 2026-04-02

- 顧客マスタの「＋ 新規追加」で、追加直後は見えるがリロード後に消える不具合を修正
- 原因だった `customers` 全件の `DELETE → POST` 方式を新規追加フローから切り離し、1件追加成功後にだけ localStorage と画面へ反映するよう変更
- 顧客取得時の整形処理をまとめ、クラウド再読込時に顧客データの形をそろえるよう整理
- Supabase REST の 1000 件上限により顧客一覧が途中までしか読めていなかったため、`cloudFetchCustomers()` をページング取得へ修正
- 本番 `customers` テーブルの同名重複 838 件を事前バックアップ後に整理し、163 件へ正常化

## 2026-04-01

- `CLAUDE.md` を新設（Claude Codeがこのプロジェクトを正しく扱うための指示書。作業前の読込ファイル順・データ保護ルール・禁止事項を明記）
- `.github/workflows/weekly-backup.yml` を追加（毎週水曜深夜0時JST に受注明細・顧客マスタをCSV出力し `backups/` フォルダへ自動保存）
- `backups/` フォルダを追加（週次バックアップの蓄積先）
- `tasks/todo.md` を追加（タスク管理ログ）
- `README.md` のファイル構成・バックアップ説明を現状に合わせて更新

## 2026-03-19

- `requirements.md` を新設
- `TEST_CHECKLIST.md` を追加
- `RUNBOOK.md` を追加
- `schema.sql` を追加し、DB初期構築の正本を分離
- Playwright による UI スモークテストを追加
- GitHub Actions の verify に UI スモークテストを追加
- 既存ドキュメントから新しい正本資料への導線を追加
- UI smoke test を拡張し、受注CRUD、検索・絞り込み保持、月切替、統計カード、印刷対象、CSV入出力、クラウドキュー回復、空マスタ同期まで自動確認できるよう整理
- GitHub Actions 上で揺れた月切替系の CSV テストを、内部変数の直接書き換えではなく UI の月送り操作ベースに安定化

## 2026-03-18

- クラウド再同期時に一覧の絞り込み条件を保持するよう修正
- 受注一覧の検索UIを `顧客検索` 1欄に統一
- 顧客検索クリアを右端の `クリア` 1ボタンに整理
- 日付解除ボタンを `日付解除` に整理
- 関連ドキュメントを更新

## 2026-03-17

- GitHub のデフォルトブランチを `main` に変更
- `main` から `master` への自動同期ワークフローを追加
- 共有クラウド設定の復元と起動フォールバックを強化
- AIツール向けの保守メモを整理

## 2026-03-13

- UUID / 文字列 ID 環境での編集・複製・削除・PDFプレビュー不具合を修正
- ブラウザ差で同期や表示が崩れる問題の調査を実施
