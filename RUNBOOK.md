# 受注管理システム 障害対応ランブック

対象時点: 2026-09-04

この文書は、障害時に何から確認するかを短時間で判断するための手順書です。

---

## 1. 最初に確認すること

1. 本番URLが正しいか
2. 右上表示がどうなっているか
3. 問題が出ているのは
   - 1台だけか
   - 1ブラウザだけか
   - 全端末共通か
4. 受注だけか、マスタもか
5. 直前に変更した内容は何か

---

## 2. 症状別の初動

### A. `接続: Cloud（エラー）` になる

1. ネットワーク接続を確認
2. ページをハードリロード
3. `cloud-config.json` の URL / anon key / enabled を確認
4. `index.html` の埋め込みフォールバックと整合しているか確認
5. Supabase が停止していないか確認
6. ローカル保存した直後なら、`接続: Cloud（エラー ・キュー:n）` のような待機状態になっていないか確認
7. エラー中でもローカル一覧の変更結果が残っていれば、まずデータ消失ではなくキュー退避を疑う

### B. PCやブラウザごとにデータが違う

1. 右上表示が全端末で `接続: Cloud（同期完了）` か確認
2. ずれている端末でハードリロード
3. `?manualCloudConfig=1` で過去に手動設定されていないか確認
4. `cloud-config.json` が意図した Supabase プロジェクトを向いているか確認
5. 受注だけか、マスタも差が出ているか切り分ける

### C. 編集・複製・削除・運行指示書の印刷が動かない

1. ブラウザ Console に構文エラーがないか確認
2. 右上表示を確認し、同期失敗ではないか切り分ける
3. `index.html` のボタンまわりに recent changes が入っていないか確認
4. `TEST_CHECKLIST.md` の受注編集系・印刷系を順に再確認する

### D. 顧客検索の一覧が勝手に戻る

1. ハードリロード
2. 古い `index.html` キャッシュを疑う
3. `TEST_CHECKLIST.md` の検索・絞り込み確認を実施
4. `applyFetchedOrders()` 周辺のフィルタ状態保持が壊れていないか確認

### E. データが0件に見える

1. クラウドが空でないか確認
2. 一時的に localStorage へ逃げていないか確認
3. CSV バックアップの有無を確認
4. 必要に応じて CSV 取込で復元

### F. マスタだけ古い、または削除したはずの項目が別端末に残る

1. 右上が `接続: Cloud（同期完了）` か確認
2. 顧客マスタと各種マスタのどちらで起きているか分ける
3. ある端末で「全削除」した場合、別端末でも空になっているか確認
4. `cloud-config.json` の接続先が意図した Supabase か確認
5. 必要に応じて `TEST_CHECKLIST.md` の同期確認を順に再実施する

---

## 3. クラウド設定変更手順

正規手順:

1. `cloud-config.json` を編集
2. `index.html` の埋め込みフォールバックも同じ値へそろえる
3. commit / push
4. GitHub Pages 反映待ち
5. 各端末でハードリロード

注意:

- 通常URLの画面から直接設定を変えない
- 緊急時のみ `?manualCloudConfig=1`
- 緊急対応後は必ず `cloud-config.json` を正本として戻す

---

## 4. Supabase停止時

1. Supabase ダッシュボードへログイン
2. 停止中プロジェクトの状態確認
3. 90日以内なら Restore project
4. 復旧後、システムをハードリロード
5. 受注件数・マスタ件数・右上表示を確認

---

## 5. 緊急時にAIへ依頼するときの渡し方

最低限、次を伝える:

- 本番URL
- 右上表示の文言
- 影響端末とブラウザ
- 受注かマスタか両方か
- `requirements.md` `SETUP.md` `CLAUDE.md` を先に読むこと

---

## 6. 復旧後の確認

- `接続: Cloud（同期完了）` になっている
- 受注件数が想定どおり
- 最新受注が見える
- マスタ更新内容が見える
- `TEST_CHECKLIST.md` の主要項目に問題がない
