# 受注管理システム 要件定義

## 1. 文書の目的

この文書は、受注管理システムが日常業務で満たすべき要件を固定するためのものです。
運用マニュアルが「どう使うか」を説明するのに対し、この文書は「何ができなければならないか」「何を壊してはいけないか」を定義します。

対象時点: 2026-04-07

---

## 2. システムの目的

- 運送業の受注情報を、複数のパソコン・複数のブラウザから共通で参照・更新できること
- 受注、顧客マスタ、各種マスタを日常業務で継続的に運用できること
- PDFプレビュー、印刷、CSV入出力など、現場の事務処理に必要な操作を一つの画面で扱えること
- 非エンジニアでも通常運用でき、障害時は AI ツールが前提を引き継いで保守しやすいこと

---

## 3. 対象範囲

### 対象機能

- 受注の登録・編集・複製・削除
- 受注一覧の検索・絞り込み・並び替え
- 顧客マスタ、積荷、荷姿、単位、ドライバー、車両の管理
- PDFプレビュー、運行指示書、引取書、CSV出力
- Supabase を用いたクラウド同期
- localStorage を用いたブラウザ側キャッシュ・補助バックアップ

### 対象外

- 複雑な権限管理やユーザー認証
- 会計システムとの双方向連携
- ネイティブアプリ化
- ブラウザ外の自動バッチ処理

---

## 4. 利用前提

- 本番URLは `https://takumitsuikebuchi-lab.github.io/order-management-system/`
- 本番公開は GitHub Pages を使用する
- クラウドデータは Supabase に保存する
- 共有クラウド設定の正本は `cloud-config.json`
- 通常運用では、利用者はクラウド設定を手入力しない
- デフォルトブランチは `main`

---

## 5. 機能要件

### 5.1 受注管理

- 利用者は受注を新規登録できること
- 利用者は既存受注を編集できること
- 利用者は既存受注を複製して新規登録できること
- 利用者は既存受注を削除できること
- 受注番号をキーとして既存受注を識別できること
- 編集・複製・削除ボタンは、主要ブラウザで安定して動作すること

### 5.2 受注一覧の表示・絞り込み

- 受注一覧は最新の受注データを表形式で表示できること
- 顧客検索は単一の入力欄で行えること
- 顧客検索欄は、自由入力による部分一致検索と候補一覧からの選択の両方に対応すること
- 顧客絞り込みは右端の `クリア` で解除できること
- 日付絞り込みは `日付解除` で解除できること
- ドライバー絞り込み、日付絞り込み、並び替えが併用できること
- バックグラウンドのクラウド再同期が走っても、表示中の絞り込み条件は維持されること

### 5.3 マスタ管理

- 顧客マスタを追加・編集・削除できること
- 積荷、荷姿、単位、ドライバー、車両の各マスタを追加・編集・削除できること
- マスタの空配列は有効データとして扱うこと
- ある端末でマスタを全削除して保存した場合、他端末でも空の状態に同期されること

### 5.4 印刷・プレビュー・CSV

- 選択した受注に対して PDF プレビューを表示できること
- 運行指示書と引取書を印刷できること
- 受注データを CSV 出力できること
- マネーフォワード請求書CSV（月次の顧客別集計）を出力できること
- CSV 取込によりデータ移行や復元ができること

### 5.5 クラウド同期

- 受注データは複数の PC・ブラウザから共通で参照・更新できること
- 顧客マスタ・各種マスタも複数の PC・ブラウザから共通で参照・更新できること
- 新しい PC や新しいブラウザでも、本番URLを開けば共有クラウド設定が自動適用されること
- クラウド同期が成功した状態は `接続: Cloud（同期完了）` として画面上で確認できること
- 通信失敗時は即時にデータを失わず、再送または再読込で収束できること

### 5.6 競合時の扱い

- 同じ受注を複数端末で開いて編集した場合、後勝ちで静かに上書きしないこと
- 別端末で先に更新済みであれば、保存時に警告して停止すること

---

## 6. 非機能要件

### 6.1 一貫性

- どの PC・どのブラウザから見ても、通常運用では同じ最新データを表示できること
- ブラウザごとの localStorage 差異によって運用結果が分岐しない構成であること

### 6.2 可用性

- 新しいブラウザで初回起動しても、共有クラウド設定が適用されて利用開始できること
- `cloud-config.json` が事故で欠落した場合でも、埋め込みフォールバックにより即時全面停止しにくいこと

### 6.3 保守性

- AI ツールが前提条件を引き継げるように、運用・要件・保守メモがリポジトリ内に存在すること
- 本番運用の正本ファイルとデフォルトブランチが明確であること
- 重要仕様は `AGENTS.md` と本書の両方で追えること

### 6.4 互換性

- Chrome、Edge、Safari など主要ブラウザで利用できること
- 特定ブラウザだけで主要操作が不能になる実装を避けること

---

## 7. 変更してはいけない重要仕様

- `cloud-config.json` は共有クラウド設定の正本である
- `index.html` の埋め込みフォールバックは保険であり、正本変更時は同じ内容へ合わせる
- 通常運用ではクラウド設定UIを自由編集させない
- 新しい端末やブラウザは、URLを開くだけで共有データに到達できることを維持する
- 一覧の絞り込み条件は、バックグラウンド同期で勝手に失われてはいけない
- 空マスタ状態は「削除漏れ」ではなく正しい業務データとして扱う

---

## 8. 障害時の基本方針

- まず `接続: Cloud（同期完了）` かどうかを確認する
- データ差異が出た場合は、UIの見え方より先に共有クラウド設定と同期状態を疑う
- 緊急保守で画面からクラウド設定を触った場合も、最終的な正本は `cloud-config.json` に戻す
- 受注やマスタの整合性を壊す修正より、表示上の問題を局所修正する方を優先する

---

## 9. 参照すべき関連文書

- `README.md`
- `SETUP.md`
- `AGENTS.md`
- `システム全体像.html`
- `運用マニュアル.html`

---

## 10. AI ツール向け運用ルール

- 改修前に `AGENTS.md`、`SETUP.md`、`requirements.md` を読むこと
- 不具合修正時は、この文書に照らして「仕様逸脱」か「仕様追加」かを先に整理すること
- 正本ファイル、共有クラウド設定、同期仕様、検索UIの保持仕様を勝手に変更しないこと
