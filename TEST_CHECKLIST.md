# 受注管理システム テストチェックリスト

対象時点: 2026-09-04

このチェックリストは、修正後に最低限確認すべき項目をそろえるためのものです。
Codex / Claude / Cursor など別の AI ツールが作業した場合も、基本的にこの順で確認します。

---

## 1. 事前確認

- 本番URLを開く
- 右上が `接続: Cloud（同期完了）` になることを確認する
- 受注一覧が表示されることを確認する
- 可能なら作業前に CSV 出力でバックアップを取る

---

## 2. 受注一覧の基本動作

- 一覧が表示される
- 件数と金額の上部集計が表示される
- 行ごとの `編集` `複製` `削除` ボタンが表示される
- チェックボックスが表示される

---

## 3. 受注の編集系

- `編集` を押して受注詳細が開く
- 1項目だけ変更して `保存` できる
- 保存後、一覧に変更が反映される
- `複製` を押して新規受注として保存できる
- `削除` を押して受注を削除できる

---

## 4. 検索・絞り込み

- `顧客検索` に文字を入力すると顧客名で絞り込める
- `顧客検索` の候補一覧から選んでも絞り込める
- `🗑 すべてクリア` で顧客・日付・ドライバー・車両・並び順の絞り込みがまとめて解除される
- `全ドライバー` の絞り込みが動く
- `全車両` の絞り込みが動く
- 並び替えを変えても一覧表示が壊れない
- 1分程度待ってクラウド再同期が走っても、絞り込み条件が勝手に消えない

---

## 5. 印刷・プレビュー

- 受注をチェックして `📄 運行指示書（選択分）` を押すと「運行指示書の設定」モーダルが開き、選択分だけが印刷対象になる
- 運行指示書が表示または印刷できる
- 引取書が表示または印刷できる

---

## 6. マスタ管理

- 顧客マスタを追加できる
- 顧客マスタを編集できる
- 顧客マスタを削除できる
- 各種マスタ（積荷・荷姿・単位・ドライバー・車両）を追加・編集・削除できる
- マスタ保存後、一覧や入力候補に反映される

---

## 7. 同期確認

- 別ブラウザまたは別PCで同じ受注が見える
- 別ブラウザまたは別PCで同じマスタが見える
- 右上表示が双方とも `接続: Cloud（同期完了）` になっている
- 新規登録した受注が別端末でも表示される
- マスタを更新した内容が別端末でも表示される
- マスタを全削除して保存した場合、別端末でも空の状態にそろう

---

## 8. 競合・例外系

- 同じ受注を2端末で開き、片方を先に保存したあと、もう片方で保存すると警告して止まる
- クラウド設定UIは通常URLでは編集できない
- `?manualCloudConfig=1` 付きURLでは保守用として開ける
- 一時的にクラウドが失敗しても、ローカル側の保存結果が消えない
- 同期回復後、`接続: Cloud（同期完了）` に戻る
- 受注がない月で `請求書CSV` を押しても安全に止まる
- CSV取込で既存受注を同じ受注番号で読み込んだ場合、重複追加ではなく上書きになる

---

## 9. 推奨確認ブラウザ

- Mac: Chrome, Safari
- Windows: Chrome, Edge

---

## 10. リリース前の最終確認

- `git diff --check` が空
- 主要ドキュメントに今回変更内容が反映されている
- 本番に不要な一時コードや調査ログが残っていない
- `main` に commit / push 済み
